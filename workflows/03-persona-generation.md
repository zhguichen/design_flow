# Workflow 3：Persona 生成 (persona-generation)

把人群原型按比例展开成 N 个具体模拟受访者，每个带五层信息结构：基础身份 / 社会处境 / 心理倾向 / 行为习惯 / 作答风格。同类下个体必须有差异，不能复制粘贴。输出 `respondents.jsonl`，每行含 `respondent_id`、`archetype_id`、五层信息。

> 占位文件，待填充（完整内容见后续 TODO-3）。
